# Apply with: oc create -f secrets.yaml
# Replace placeholder values before applying
apiVersion: v1
kind: Secret
metadata:
  name: nrms-secrets
  labels:
    app: nrms
type: Opaque
stringData:
  database-url: "postgresql://nrms:CHANGE_ME@postgresql:5432/nrms?schema=public"
  jwt-secret: "CHANGE_ME_TO_RANDOM_STRING"
