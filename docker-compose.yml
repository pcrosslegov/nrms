services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nrms
      POSTGRES_USER: nrms
      POSTGRES_PASSWORD: nrms_dev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
